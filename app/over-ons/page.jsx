import { OverOnsContent } from '../../components/site';

export const metadata = {
  title: 'Over ons — PS-Connect',
  description: 'Drie partners, één team. Familiebedrijf in Noord-Holland sinds 2022. Pascal, Mark en Matthijs.',
};

export default function OverOnsPage() {
  return <OverOnsContent />;
}
