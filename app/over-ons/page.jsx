import { OverOnsContent } from '../../components/site';

export const metadata = {
  title: 'Over ons — PS-Connect',
  description: 'Tegengeluid in de uitzendwereld. Familiebedrijf in Noord-Holland Noord sinds 2022 — dichtbij, persoonlijk, eerlijk naar werkgever én flexkracht. Pascal, Mark en Matthijs.',
};

export default function OverOnsPage() {
  return <OverOnsContent />;
}
