import { CasesContent } from '../../components/site';

export const metadata = {
  title: 'Cases — PS-Connect',
  description: 'Voorbeeldcases uit openteelt en (glas)tuinbouw, logistiek & distributie, en facilitair. Wat wij voor onze klanten in Noord-Holland Noord doen.',
};

export default function CasesPage() {
  return <CasesContent />;
}
