import { ContactContent } from '../../components/site';

export const metadata = {
  title: 'Contact — PS-Connect',
  description: 'Offerte aanvragen of inschrijven? Bel direct of vul het formulier in. Pascal of Mark belt dezelfde werkdag terug.',
};

export default function ContactPage() {
  return <ContactContent />;
}
